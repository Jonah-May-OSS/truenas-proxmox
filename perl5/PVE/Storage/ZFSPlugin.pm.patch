--- a/perl5/PVE/Storage/ZFSPlugin.pm
+++ b/perl5/PVE/Storage/ZFSPlugin.pm
@@ -180,6 +180,14 @@ sub type {
         iscsiprovider => {
             description => "iscsi provider",
             type        => 'string',
+        },

+        # — TrueNAS API integration —
+        truenas_user       => {
+            description => "TrueNAS API Username",
+            type        => 'string',
+            optional    => 1,
+        },
+        truenas_secret     => {
+            description => "TrueNAS API Password or Token",
+            type        => 'string',
+            format      => 'password',
+            optional    => 1,
+        },
+        truenas_token_auth => {
+            description => "Use token (Bearer) authentication",
+            type        => 'boolean',
+            default     => 0,
+            optional    => 1,
+        },
+        truenas_use_ssl    => {
+            description => "Connect to TrueNAS API over HTTPS",
+            type        => 'boolean',
+            default     => 1,
+            optional    => 1,
+        },
+        truenas_apiv4_host => {
+            description => "TrueNAS API host (overrides portal)",
+            type        => 'string',
+            optional    => 1,
         },
         # this will disable write caching on comstar and istgt.
         # it is not implemented for iet. iet blockio always operates with
@@ -230,7 +238,7 @@ sub type {
         sparse         => { optional => 1 },
         comstar_hg     => { optional => 1 },
         comstar_tg     => { optional => 1 },
-        truenas_user   => { optional => 1 },
+        # (no further repetition needed—already declared above)
         truenas_secret => { optional => 1 },
         truenas_token_auth => { optional => 1 },
         truenas_use_ssl    => { optional => 1 },
